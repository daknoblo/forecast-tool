name: Release

on:
  push:
    branches: [main]
    tags: ["v*"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Gate the release on a green build. ci.yml runs in parallel on the same push,
  # so without this job a failing test would still publish a new :latest image.
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: go vet
        run: go vet ./...

      - name: Test (race detector)
        run: go test -race ./...

  release:
    name: Build, Push & Sign
    needs: verify
    runs-on: ubuntu-latest
    outputs:
      image: ${{ steps.image.outputs.name }}
      digest: ${{ steps.build.outputs.digest }}
    permissions:
      contents: read
      packages: write
      security-events: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # The version stamp is derived from the latest tag, so the tags have to
          # be there.
          fetch-depth: 0
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,format=short

      - name: Lowercase image name
        id: image
        run: echo "name=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      # Stamped into the binary and shown in the app footer. A tag build carries
      # the tag itself; a build from main names the release it builds on and
      # marks that it is ahead of it.
      - name: Resolve version
        id: version
        run: |
          if [ "$GITHUB_REF_TYPE" = "tag" ]; then
            echo "value=$GITHUB_REF_NAME" >> "$GITHUB_OUTPUT"
          else
            tag="$(git describe --tags --abbrev=0 2>/dev/null || echo untagged)"
            echo "value=$tag+main" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ steps.version.outputs.value }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: true
          sbom: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Sign image (keyless)
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ steps.image.outputs.name }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: cosign sign --yes "${IMAGE}@${DIGEST}"

  # The image is published for both architectures, so both get scanned: `trivy
  # image` resolves a multi-arch digest to the platform it runs on, which would
  # leave arm64 unexamined. Each platform uploads under its own SARIF category,
  # otherwise the second upload would replace the first.
  scan:
    name: Scan ${{ matrix.platform }}
    needs: release
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Trivy vulnerability scan
        uses: aquasecurity/trivy-action@v0.36.0
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TRIVY_PLATFORM: ${{ matrix.platform }}
        with:
          image-ref: ${{ env.REGISTRY }}/${{ needs.release.outputs.image }}@${{ needs.release.outputs.digest }}
          format: sarif
          output: trivy-results.sarif
          severity: CRITICAL,HIGH
          ignore-unfixed: true

      # A clean scan produces an empty SARIF and therefore no entry anywhere in
      # the Security tab, which is indistinguishable from a scan that never ran.
      # The summary states which of the two it was.
      - name: Summarise scan
        if: always()
        env:
          IMAGE: ${{ env.REGISTRY }}/${{ needs.release.outputs.image }}@${{ needs.release.outputs.digest }}
          PLATFORM: ${{ matrix.platform }}
        run: |
          set -euo pipefail
          {
            echo "### Trivy · ${PLATFORM}"
            echo
            echo "Image: \`${IMAGE}\`"
            echo "Filter: CRITICAL, HIGH · nur behebbare Funde"
            echo
            if [ ! -f trivy-results.sarif ]; then
              echo "> [!WARNING]"
              echo "> Trivy hat keinen Report erzeugt – der Scan ist fehlgeschlagen."
            else
              n="$(jq '[.runs[].results[]] | length' trivy-results.sarif)"
              echo "Ergebnis: **${n} Fund(e)**"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload SARIF to Security tab
        if: ${{ always() && hashFiles('trivy-results.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-results.sarif
          category: trivy-${{ matrix.platform }}

  # A version tag also publishes the GitHub release entry, so tagging is the
  # only manual step of a release.
  github-release:
    name: GitHub Release
    needs: release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          # The annotated tag message becomes the release body, so the full
          # history and the tag objects have to be present.
          fetch-depth: 0
          persist-credentials: false

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
          IMAGE: ${{ env.REGISTRY }}/${{ github.repository }}
        run: |
          set -euo pipefail
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "release $TAG already exists, nothing to do"
            exit 0
          fi
          {
            git tag -l --format='%(contents:body)' "$TAG"
            printf '\n## Container image\n\n```\ndocker pull %s:%s\n```\n' "${IMAGE,,}" "${TAG#v}"
          } > notes.md
          gh release create "$TAG" --verify-tag --title "$TAG" \
            --notes-file notes.md --generate-notes
